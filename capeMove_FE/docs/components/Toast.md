# Toast (UI Slice + Toast component)

- pushToast({ id: string, message: string, severity: 'info'|'success'|'warning'|'error' })
  Adds a message to the toast queue.

- popToast()
  Removes the oldest toast.

Usage:
