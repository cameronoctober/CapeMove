# Example environment variables for CapeMove frontend (Vite)
VITE_API_BASE_URL=http://localhost:8080
VITE_WS_BASE_URL=ws://localhost:8080
VITE_PWA_SW_SCOPE=/
VITE_APP_NAME=CapeMove

# Notes:
# Server should set httpOnly refresh cookie at /api/auth/login when possible;
# fallback to localStorage key 'cape_refresh' is supported in client code if server doesn't set cookie.
